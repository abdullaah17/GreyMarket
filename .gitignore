__pycache__/
*.pyc
.venv/

# run artifacts
# NOTE: parts.csv is NOT ignored. It was a discover output when discover was
# the primary input path; it is now a hand-curated, sourced input and the most
# expensive artifact in the repo (see CURATION.md). It is tracked deliberately.
listings.csv

# credentials -- never commit; pass via environment
.env
.env.*
!.env.example
*.pem

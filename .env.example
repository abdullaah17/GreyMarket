# Copy to .env and fill in. .env is gitignored; never commit real values.
#   set -a; source .env; set +a
# Nexar portal -> your app -> credentials. The app must have a Supply API
# subscription attached, or every query returns "part limit of 0".
NEXAR_CLIENT_ID=
NEXAR_CLIENT_SECRET=
